{
  "name": "healthcore-programming-fundamentals",
  "version": "1.0.0",
  "private": true,
  "description": "Milestone 2 TypeScript logic for HealthCore operations",
  "scripts": {
    "build": "npx esbuild main.ts --bundle --format=esm --platform=browser --outfile=main.js",
    "typecheck": "npx tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "typescript": "^5.6.3"
  }
}
